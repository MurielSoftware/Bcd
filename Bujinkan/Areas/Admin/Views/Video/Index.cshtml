@using Client.Core.HtmlHelpers
@using Shared.Core.Constants
@using Shared.I18n.Constants
@using Shared.I18n.Utils

@{
    ViewBag.Title = Html.LocalizedDisplay(MessageKeyConstants.LABEL_VIDEO);
}

@section ribbon {
    <ul class="ribbon-group">
        <li>
            @Html.DialogActionLink("<i class='" + @HtmlConstants.FA_IMAGE_VIDEO + "'></i>" + Html.LocalizedDisplay(MessageKeyConstants.LABEL_ADD_LINK), HtmlConstants.DIALOG_VIDEO, WebConstants.VIEW_CREATE, WebConstants.CONTROLLER_VIDEO, null, new { @class = "ribbon-button" })
        </li>
        <li>
            @Html.LocalizedActionLink(@"<i class='" + HtmlConstants.FA_IMAGE_CATEGORY + "'></i> " + ResourceUtils.GetString(MessageKeyConstants.LABEL_CATEGORY), WebConstants.VIEW_INDEX, WebConstants.CONTROLLER_LINK_CATEGORY, null, new { @class = "ribbon-button" })
        </li>
        <li class="ribbon-group-title">
            @Html.LocalizedDisplay(MessageKeyConstants.LABEL_LINKS)
        </li>
    </ul>
}

@section header{
    <div class="header-section-title">
        <h1><i class="@HtmlConstants.FA_IMAGE_VIDEO"></i> @Html.LocalizedDisplay(MessageKeyConstants.LABEL_VIDEO)</h1>
        <small>@Html.LocalizedDisplay(MessageKeyConstants.LABEL_VIDEO)</small>
    </div>
    <img src="/Content/images/admin/admin-cover.jpg" />
}

@section breadcrumb {
    <ul class="breadcrumb">
        <li>@Html.LocalizedDisplay(MessageKeyConstants.LABEL_VIDEO)</li>
        <li>@Html.LocalizedActionLink(MessageKeyConstants.LABEL_VIDEO, WebConstants.VIEW_INDEX, WebConstants.CONTROLLER_VIDEO)</li>
    </ul>
}

<div class="panel">
    <div class="panel-heading">
        <ul class="nav nav-tabs">
            <li class="active">
                <a href="#form" data-toggle="tab">
                    <i class="fa fa-list"></i> @Html.LocalizedDisplay(MessageKeyConstants.LABEL_VIDEO)
                </a>
            </li>
        </ul>
    </div>
    <div class="panel-body">
        @Ajax.RemoteContent(WebConstants.VIEW_PAGED_LIST, WebConstants.CONTROLLER_VIDEO, WebConstants.AREA_ADMIN)
    </div>
</div>